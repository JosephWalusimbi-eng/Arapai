Create the folder "models" and subfolders in it "lite" "standard" "advanced"
Create the virtual environment "venv"
Install dependencies from requirements.txt
Run the app on windows, "streamlit run app.py"
