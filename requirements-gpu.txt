# GPU-backed llama-cpp-python (optional).
# Use ONLY after setting CMAKE_ARGS and with CUDA installed. See howtorun.txt.
# Install: set CMAKE_ARGS=-DGGML_CUDA=on then pip install -r requirements-gpu.txt --no-cache-dir --force-reinstall

llama-cpp-python==0.2.20
