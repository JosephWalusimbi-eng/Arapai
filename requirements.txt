# Arapai – one-command install on any device (CPU).
# From project root with venv activated: pip install -r requirements.txt
# Optional GPU build: see howtorun.txt and use requirements-gpu.txt with CMAKE_ARGS set.

streamlit==1.31.1
llama-cpp-python==0.2.20
numpy
pypdf
faiss-cpu
psutil

# RAG stack (PINNED)
huggingface-hub==0.19.4
transformers==4.38.2
sentence-transformers==2.6.1
